# Copy this file to NoteChat/Env/.env (do not commit the real file)
# Keys are used only in Debug to prime Keychain on first app launch.

OPENAI_API_KEY="sk-your-openai-key"
ANTHROPIC_API_KEY="sk-your-anthropic-key"
GOOGLE_API_KEY="your-google-key"
XAI_API_KEY="xai-your-key"

# Optional model defaults (leave blank to use app defaults)
# DEFAULT_PROVIDER="openai"
# DEFAULT_MODEL="gpt-4o-mini"
